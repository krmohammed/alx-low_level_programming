#include <stdio.h>
/**
 * main - entry point
 * Description - prints all digits of base 10
 * Return: always 0
 */

int main(void)
{
	int i = 0;

	while (i < 0)
	{
		printf("%d", i);
		i++;
	}
	printf("\n");

	return (0);
}
