#include <stdio.h>

/**
 * main - prints the number of cmd line args
 * @argc: argument count
 * @argv: argument vector
 *
 * Return: 0 (Always)
 */

int main(int argc, char *argv[])
{
	(void)argv;

	printf("%d\n", argc);

	return (0);
}
